-# in app/views/search/index.html.haml

%h1 Display search results with optional category filtering

- if @results.any?
  %ul
    - @results.each do |item|
      = link_to item.title, item_path(item)
      %img{ src: item.images.first.data_uri, alt: item.title }
- else
  %p No items found
